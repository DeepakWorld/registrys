{
    "name": "namingo/registry-api",
    "description": "Namingo Registry API for Vercel",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "monolog/monolog": "^3.7",
        "namingo/rately": "^0.1.0"
    },
    "config": {
        "platform": {
            "php": "8.2.0"
        },
        "allow-plugins": {
            "composer/package-versions-deprecated": true
        }
    },
    "autoload": {
        "files": [
            "helpers.php"
        ]
    }
}